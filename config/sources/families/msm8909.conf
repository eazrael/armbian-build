# --- Kernelquelle direkt von GitHub ---
# TODO: <DEIN_GH_USER>/<DEIN_REPO> und <branch|tag|commit> anpassen
# declare -g KERNELSOURCE='https://github.com/msm8916-mainline/linux.git'
# declare -g KERNELBRANCH='tag:6.12.1'        # alternativ: branch:<name> oder tag:<vX.Y.Z>
# declare -g KERNEL_MAJOR_MINOR='6.12'

# # Keine Armbian-Patches einmischen
# declare -g KERNELPATCHDIR=''

# (Optional) Toolchain/Flags feinjustieren:
# KERNEL_CONFIGURE="yes"            # interaktives menuconfig beim Build
# KERNEL_GIT_DEPTH=0                # voller Clone (nützlich für bisect)
